[Unit]
Description=Goy Mesh Network Coordination Server
After=network.target

[Service]
Type=simple
User=coord
Group=coord
WorkingDirectory=/var/lib/coord-server
ExecStart=/usr/local/bin/coord-server --config /etc/coord-server/config.toml
Restart=on-failure
RestartSec=5s

# Security restrictions
ProtectSystem=full
ProtectHome=true
NoNewPrivileges=true
PrivateTmp=true

# Environment variables (can be overridden via /etc/default/coord-server)
EnvironmentFile=-/etc/default/coord-server

[Install]
WantedBy=multi-user.target
